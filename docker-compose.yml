version: '2'
volumes:
  resin-data:
services:
  nats-streaming-server:
    # privileged: true
    restart: no
    build: ./nats-streaming-server
    ports:
      - "8422:4222"
      - "8822:8222"
      - "8622:6222"
    volumes:
      - 'resin-data:/data'
#  influxdb:
#    restart: always
#    build: ./influxdb
#    volumes:
#      - 'resin-data:/data'
#  grafana:
#    restart: always
#    build: ./grafana
#    ports:
#      - "80:3000"
#    volumes:
#      - 'resin-data:/data'
